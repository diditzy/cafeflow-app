# =====================================================
# Windows Configuration
# =====================================================
# Konfigurasi untuk Windows dengan SQL Server lokal
# File ini akan otomatis digunakan di Windows
# =====================================================

# Server Configuration
db.server=localhost
db.port=1433
db.database=CafeFlowDB

# SQL Server Authentication (Windows)
db.username=cafeflow_admin
db.password=CafeFlow2025

# Connection Settings
db.integratedSecurity=false
db.encrypt=false
db.trustServerCertificate=true
