# =====================================================
# Default Database Configuration
# =====================================================
# File ini akan otomatis memilih konfigurasi berdasarkan OS:
# - Windows: db-windows.properties
# - Linux: db-linux.properties
# - Override: db-local.properties (tidak di-commit ke Git)
#
# Untuk setup:
# 1. Copy db-windows.properties atau db-linux.properties
# 2. Rename jadi db-local.properties
# 3. Edit sesuai konfigurasi lokal Anda
# =====================================================

# Default fallback configuration
db.server=localhost
db.port=1433
db.database=CafeFlowDB
db.username=cafeflow_admin
db.password=CafeFlow2025
db.integratedSecurity=true
db.encrypt=false
db.trustServerCertificate=true